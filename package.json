{
  "name": "wolframalpha",
  "version": "1.0.0",
  "description": "",
  "main": "./distribution/CustomWolframAlpha.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir distribution && node ./distribution/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0",
    "request": "^2.72.0",
    "xml-parser": "^1.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0"
  }
}
